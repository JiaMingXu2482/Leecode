[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "24"
  NEXT_TELEMETRY_DISABLED = "1"
  DATABASE_URL = "file:./netlify-build.db"

[dev]
  command = "npm run dev"
  targetPort = 3000
  port = 8888
  framework = "#auto"
