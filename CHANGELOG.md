# 更新日志

Hot100 复习计划（Ebbinghaus Planner）—— 自建的间隔重复刷题管理站。

版本号对应界面左下角显示的 `APP_VERSION`。日期是提交日期。
本文件按版本倒序，只记录用得着的改动，重构和纯内部整理不单独列。

---

## v1.19.0 — 2026-08-12

### 新增
- **算法总结页面**（`/algo-notes`）。按分类归档的 Markdown 笔记，和具体某道题解耦，用来沉淀「DP 有哪几类、每类的模板长什么样」这种跨题的东西。
  - 自写的轻量 Markdown 渲染器（`src/lib/markdown.ts`）：标题、围栏代码块、表格、有序/无序列表、引用、分隔线、粗体/斜体/删除线、行内代码、链接。**没有引 marked / markdown-it** —— 线上是 docker build 里跑 `npm ci`，加依赖要重新生成 lockfile，而本项目在 Windows 生成、Linux 部署，之前踩过 `EUSAGE`。
  - **题号自动链接**：正文里的 `#53`、`HJ14`、`P2352` 以及裸数字会变成指向题目详情页的链接。裸数字的边界卡得很严，`第1阶段`、`3-5道`、`70%`、`≤20` 不会被误伤（这几个数字本身都是 Hot100 题号，专门写了回归测试）。代码块内一律不链，否则 `dp[i-1]` 会被乱链。
  - **反向链接**：题目详情页新增「相关算法总结」，列出正文引用过这道题的笔记。
  - 全文搜索（标题 / 分类 / 正文）、长文自动生成目录、代码块一键复制。
  - 编辑器复用现有 Monaco（加了 `language` / `height` 参数），左边写、右边实时预览。

### 内部
- 新增 `AlgoNote` 表；`/problems/by-number/[frontendId]` 路由负责把题号跳到题目详情页。
- Markdown 渲染器 22 个测试，代码块复制 9 个 jsdom 测试。

---

## v1.18.0 — 2026-08-11

### 变更
- **速成题单分类内部改成由易到难**（100 → 200 → 300 分）。分类之间仍按作者编排的顺序走（动态规划 → 模拟 → 贪心 → DFS → BFS → …），一类刷干净才进下一类。原来照抄文档顺序会在第二天就撞上 300 分压轴题。
- **Hot100 复习新增「考点匹配」模式**（`reviewMode=TOPIC`）：不再按到期日排，而是挑和当天新题考点相近的题，让新学的和复习的落在同一个知识点上。同考点内按「有多不熟」（历史平均反馈分）排序，过期时间只作为最后的并列条件。牛客 / 速成题单的复习仍走遗忘曲线。
- **新增每日时间预算**（复用 `defaultDailyMinutes`）：新题先占额度，复习吃剩下的时间。所以「每天几小时」这一个数就能控制复习量，不用再单独设条数。
- **新增休息日设置**（`restWeekdays`，ISO 星期数）：这些天完全不排题，周计划里显示「休息日」。
- 周计划每道题的题号下面显示题型，一眼能看出当天新题和配套复习是不是同一个考点。
- `/api/settings` 支持调整每日时长、休息日、复习模式，方便按周切换强度。

### 修复
- **重排本周不再重复排题**：重排只从今天往后走，本周已过去几天里没做完的条目没进「已占用」集合，同一道题会在周一和周三各排一遍。现在重排时先清掉过去几天的未完成条目，让它们重新参与排题（已完成的一条不动）。
- **给 ACM 复习限量**：集中刷完一批题单题后它们会同时到期，把当天的时间预算吃光，Hot100 一道都排不进来 —— 而开启考点匹配的目的正是补 Hot100。考点匹配模式下 ACM 到期复习每天最多 2 道。

---

## v1.17.0 — 2026-08-05

### 新增
- **接入塔子哥「华为机考一周速成题单」**（CodeFun2000，69 道）。题单优先于牛客 HJ，刷完自动回落。刷题计划页拆成三个 Tab（速成题单 / 牛客华为机试 / LeetCode Hot100），各自独立统计进度。题号显示为 `P2352`，链接指向 codefun2000.com。

---

## v1.16.0 — 2026-08-04

### 变更
- 每天最多排一道简单题，其余名额优先中等题，其次困难题。配额优先于难度配比：宁可多排一道简单题，也不少排。

---

## v1.15.0 — 2026-08-04

### 修复
- **按真实算法给牛客 HJ 题重新分类**。之前直接取牛客列表页的第一个标签，导致 108 道题里 51 道被归进「字符串」，动态规划 / DFS / BFS 几乎排不出题。重新分成 13 类，并同步更新已入库题目的 tags。
- 分批补题时也会避开当天已有的题型，不会出现一天三道「数组与排序」。
- 修正牛客题号显示与外链。

---

## v1.14.x — 2026-08-02 ~ 08-04

### 新增
- **牛客华为机试题库（HJ1–108）并入刷题计划**，作为 ACM 格式的新题来源；LeetCode 转为只出复习题。多题库通过 `Problem.source` + `displayId` 区分，题号段互不重叠（LeetCode 1–999、牛客 10001–10108）。统计按题库分开计算。

### 移除
- ACM 笔记页（v1.13.0 加的）随之下线，相关表和数据一并清理。

### 修复
- 热力图恢复成 18 周定宽方块，并去掉下方多出来的滚动条。

---

## v1.13.0 — 2026-07-30

### 新增
- ACM 笔记页：可搜索的代码模板知识库（后于 v1.14.0 移除，功能并回题目笔记）。

---

## v1.12.x — 2026-07-13 ~ 07-14

### 新增
- **限制欠账补课量**：跳过一天之后，第二天最多叠加 2 道顺延新题、10 道复习，不会出现一天几十题。
- 今日任务把新题排在列表最前面；移除与周计划重复的「添加一题」按钮。

### 修复
- **手动拖动优先级最高**：某天一旦被自动填充过，用户把题拖走或删掉后系统不再自动补回来。

---

## v1.10.x ~ v1.11 — 2026-07-09 ~ 07-10

### 新增
- 未做完的新题自动顺延到今天；计划助手加入可持久化的「人格」与长期记忆。

### 修复
- **后端完整性审计**：修掉计划分钟数漂移、事务原子性、请求超时、死代码等 9 处问题。
- 计划助手改为读取真实计划数据，不再编造；并且遵守每日新题配额。
- 单日例外不再改写默认配额。

---

## v1.9.0 ~ v1.10.0 — 2026-07-08 ~ 07-09

### 新增
- **优先类别排题** + **DeepSeek 计划助手**（支持函数调用，可直接改计划）。
- 助手窗口可拖动、聊天记录持久化；移除「重排本周」按钮，改由助手驱动。

---

## v1.8.0 — 2026-07-07

### 变更
- 笔记编辑器换成 Monaco（力扣同款引擎），资源自托管 —— 默认的 jsdelivr CDN 在国内不稳。
- 「新题」的定义改为「本应用内没做过」，历史 AC 不算。
- 复习间隔改为按掌握度动态计算。

---

## v1.6.x ~ v1.7 — 2026-07-01 ~ 07-06

### 新增
- 拖拽式周计划 + 题目搜索；每日计划自我修复（打开页面即补齐到期复习和当日配额）；数据备份导出；笔记草稿。
- 每天新题从 3 道提升到 4 道。

### 性能
- 按视图分别加载数据、并行查询、路由预取与会话级缓存，去掉加载屏。

---

## v0.x — 2026-06-23 ~ 06-30

初版到基本可用：

- Hot100 题库与艾宾浩斯复习调度、每日 / 每周计划视图。
- 访问密码保护、深浅色主题、响应式布局与可折叠侧边栏。
- 做题反馈评分（0 = AC 快 … 5 = 陌生）、解题思路 / C++ 语法双栏笔记、历史笔记页。
- leetcode.cn 同步（AC 状态、提交画像、最近代码）。
- 学习热力图、统计页、按题型分组的刷题计划。
